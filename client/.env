# API: omit in local dev so requests stay on the Vite origin and proxy to the API (fixes CORS).
# Set for production builds, e.g. VITE_API_URL=https://your-api.example.com
# VITE_API_URL=http://localhost:5000
